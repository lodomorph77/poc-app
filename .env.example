# Copy to .env and fill in values before running locally
JWT_SECRET=super-secret-jwt-key-change-me

# Set these when tagging images for GHCR (not needed for plain docker compose build)
IMAGE_OWNER=lodomorph77
IMAGE_TAG=latest
